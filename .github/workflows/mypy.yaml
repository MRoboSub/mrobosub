name: Run mypy

on:
  workflow_dispatch:

  push:
    paths:
      - "*.py"
      - ".github/workflows/mypy.yaml"
      - "**/mypi.ini"

  pull_request:
    paths:
      - "*.py"
      - ".github/workflows/mypy.yaml"
      - "**/mypi.ini"

jobs:
  mypy:
    runs-on: ubuntu-latest
    container:
      image: mrobosub/mrobosub:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run mypy
        run: mypy mrobosub_planning
